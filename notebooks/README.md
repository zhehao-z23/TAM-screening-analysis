# Notebook Templates

Keep reusable notebook templates here. Per-batch generated notebooks should generally stay outside Git unless they are minimal, cleaned, and useful as examples.

The canonical workflow is implemented in `scripts/run_fixed6_analysis.py`; notebooks should call or mirror that script rather than becoming the only source of truth.
